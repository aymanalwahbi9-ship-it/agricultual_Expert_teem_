# حزمة عمر الحيقي — الواجهات والسجل والتقارير

تحتوي على Controllers وRequests وRoutes وBlade وCSS وFeature Tests الفعلية.

```powershell
git checkout main
git pull origin main
git checkout -b feature/web-interface
composer install
npm install
npm run build
php artisan route:list
php artisan test
vendor/bin/pint
git add app/Http resources routes tests/Feature
 git commit -m "Complete Arabic interfaces history and reports"
git push -u origin feature/web-interface
```
